# Контроль дублей — ночная проверка объявлений

Скрипт `owners_checker.py` ходит в Supabase (таблица `owners`), запускает парсер `https://parser-links-production.up.railway.app/check` для каждого объявления и:

- отмечает запись как проверенную (`checked=true`);
- удаляет объект из `owners` и `objects`, если объявление снято (парсер вернул `status=false`);
- сравнивает цену c таблицей `objects` и понижает её, когда на площадке она стала ниже;
- при повышении цены оставляет старое значение и отправляет уведомление в Telegram.

## Подготовка

1. **Python окружение**
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   ```
2. **Supabase**
   ```sql
   alter table owners add column if not exists checked boolean default false;
   ```
3. **Заполните `.env`** (см. `.env.example`). Обязательные переменные:
   - `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`;
   - `PARSER_CHECK_URL`;
   - `NOTIFY_BOT_TOKEN`, `NOTIFY_CHAT_ID` (если нужны уведомления).

## Запуск

```bash
python owners_checker.py            # фон, крутится бесконечно
python owners_checker.py --run-now  # одиночный проход по всем unchecked
python owners_checker.py --reset-only  # сбросить checked=false и выйти
```

По умолчанию скрипт выбирает случайное время внутри окна `CHECK_WINDOW_START_HOUR..CHECK_WINDOW_END_HOUR` (например, 01:00–04:00) и запускает полный цикл. Сброс флага `checked` происходит в `OWNERS_CHECK_RESET_HOUR` (обычно 00:00).

## Карантин дублей (objects → owners)

Отдельный скрипт `duplicates_checker.py` работает только с таблицей `objects` и ищет потенциальные дубли по адресу, количеству комнат, этажу, общей площади и цене (значения приводятся к «корзинам» — шаг цены `DUPLICATES_PRICE_BUCKET`, шаг площади `DUPLICATES_AREA_BUCKET`). Если находится кластер из **минимум двух разных** `owners_id`, формируется группа в таблице `duplicates` и в Telegram прилетает сообщение вида:

```
⚠️ Возможный дубль (группа 1234abcd):
62 — https://... (owners_id 11, 70 м², 200 000 ₽, ... )
63 — https://... (owners_id 15, 70 м², 200 000 ₽, ... )
[🧷 Слить в 11] [🧷 Слить в 15] [❌ Игнорировать]
```

- `🧷 Слить в N` — оставляет выбранный `owners_id=N`, остальные владельцы и связанные с ними строки `objects` удаляются; статус группы становится `resolved`.
- `❌ Игнорировать` — помечает группу как `ignored`, кнопки исчезают.

Ночная проверка (`owners_checker.py`) не менялась, поэтому лог в консоли теперь легко отличить: `[owners-checker]` для актуальности и `[duplicates]` для карантина дублей.

### База и запуск

1. Создайте таблицу `duplicates` (минимум колонки: `id bigserial`, `owners_id bigint`, `duplicate_group uuid`, `duplicate_status text`, `created_at timestamptz default now()`, `resolved_at timestamptz`).
2. Заполните `.env` (см. раздел ниже — блок `Duplicate monitor`). Если отдельного бота/чата нет, скрипт использует `NOTIFY_*`.
3. Запуск:
   ```bash
   python duplicates_checker.py --run-once   # разовый прогон
   python duplicates_checker.py              # постоянный мониторинг + long polling кнопок
   ```

Скрипт каждые `DUPLICATES_SCAN_INTERVAL_SECONDS` минут перечитывает `objects`, а Telegram-поллинг идёт раз в `DUPLICATES_TELEGRAM_POLL_SECONDS`. Чтобы не плодить одинаковые уведомления, пока группа находится в статусе `pending`, новые записи с теми же владельцами не создаются.

### Что происходит в цикле

1. Вытягиваются батчи (`CHECK_BATCH_SIZE`) записей, где `checked=false/null`.
2. Для каждой записи определяется источник (Avito — не чаще, чем раз в `AVITO_REQUEST_INTERVAL_SECONDS`, Cian — без задержек).
3. Парсер вызывается с полями `url`, `owner_id`, `source`.
4. Ответ сохраняется в колонку `parsed`, флаг `checked` и `status` обновляются.
5. Если `status=false`, объект удаляется из `owners` и `objects`.
6. Если в ответе есть цена и она упала — через `OBJECTS_LINK_COLUMN` обновляем `objects.price`, событие попадает в утренний отчёт; если выросла — добавляется строка вида `📈 id 7 — цена изменилась ...`.
7. Когда объявление становится неактуальным (либо `owners.status=false`, либо парсер вернул `status=false`/`"unpublished"`), запись удаляется из `owners` и `objects` по `id`; в утреннем отчёте появляется строка `❌ id 7 — объявление больше не актуально`.
8. К `SUMMARY_REPORT_HOUR` (учитывается `SUMMARY_TZ_OFFSET` относительно UTC) отправляется единый отчёт: в начале сводка (`Проверено`, `Удалено`, список id со снижениями/повышениями), затем полный список событий.

## Продакшн

- Держите процесс через `systemd`, `pm2` или `supervisor`.
- Убедитесь, что машина имеет доступ к Supabase и Railway.
- Логи выводятся в stdout; уровень можно задать `OWNERS_CHECK_LOGLEVEL` (INFO/DEBUG/ERROR).
- **Railway:** репозиторий уже готов к деплою. Достаточно:
  1. Подключить GitHub-репозиторий `control-doubles-checker`.
  2. В Variables Railway перенести значения из `.env`.
  3. Railway автоматически запустит процесс с командой из `Procfile` (`worker: python owners_checker.py`).

## Переменные окружения

| Блок | Переменная | Назначение |
| --- | --- | --- |
| Supabase | `SUPABASE_URL` | Базовый URL проекта |
| | `SUPABASE_SERVICE_ROLE_KEY` | Service Role Key |
| Данные | `OWNERS_TABLE` | Таблица владельцев |
| | `OWNERS_CHECK_COLUMN` | Флаг «проверено» |
| | `OWNERS_CHECK_RESET_HOUR` | Час суточного сброса |
| | `OWNERS_LINK_COLUMN` | Колонка в `owners`, которая используется как идентификатор (`id` по умолчанию) |
| | `OBJECTS_TABLE` | Таблица объектов |
| | `OBJECTS_PRICE_COLUMN` | Колонка с ценой |
| | `OBJECTS_LINK_COLUMN` | Колонка в `objects`, по которой ищем запись (`owners_id` для связи по идентификатору владельца) |
| Парсинг | `PARSER_CHECK_URL` | Endpoint Railway |
| | `CHECK_WINDOW_START_HOUR` / `CHECK_WINDOW_END_HOUR` | Ночное окно |
| | `AVITO_REQUEST_INTERVAL_SECONDS` | Задержка между запросами Avito |
| | `CIAN_REQUEST_INTERVAL_SECONDS` | Задержка для Циан (0 = без пауз) |
| | `CHECK_BATCH_SIZE` | Размер батча |
| | `SUMMARY_REPORT_HOUR` | Час, когда отправлять агрегированный отчёт |
| | `SUMMARY_TZ_OFFSET` | Смещение относительно UTC (например, `3` для Москвы) |
| Telegram | `NOTIFY_BOT_TOKEN` | Токен бота, который шлёт уведомления (снижение/рост цены, снятие объявления) |
| | `NOTIFY_CHAT_ID` | Чат/топик (ID) |
| | `NOTIFY_THREAD_ID` | ID темы (опционально) |
| | `TELEGRAM_PROXY_URL` | Прокси (если нужен) |
| Прочее | `OWNERS_CHECK_LOGLEVEL` | INFO/DEBUG/ERROR |
| Duplicate monitor | `DUPLICATES_TABLE` | Имя таблицы для карантина (по умолчанию `duplicates`) |
| | `DUPLICATES_SCAN_INTERVAL_SECONDS` | Периодичность повторных сканов `objects` |
| | `DUPLICATES_TELEGRAM_POLL_SECONDS` | Частота long polling Telegram |
| | `DUPLICATES_PRICE_BUCKET` | Шаг цены для группировки (например, 5000 ₽) |
| | `DUPLICATES_AREA_BUCKET` | Шаг площади (м²) |
| | `DUPLICATES_MIN_GROUP_SIZE` | Минимальное число объектов в кластере |
| | `DUPLICATES_PAGE_SIZE` | Порция чтения `objects` по API |
| | `DUPLICATES_LOGLEVEL` | INFO/DEBUG/ERROR для `duplicates_checker.py` |
| | `DUPLICATES_BOT_TOKEN` / `DUPLICATES_CHAT_ID` / `DUPLICATES_THREAD_ID` | Переопределение Telegram реквизитов (если отличны от `NOTIFY_*`) |
| | `DUPLICATES_OFFSET_FILE` | (опция) файл для хранения `update_id` |

Все переменные уже описаны в `.env.example`.
